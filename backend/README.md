# Backend

Simple Flask API server that communicates with the React frontend.

## Setup

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python app.py
```

Server runs on `http://localhost:5000`
